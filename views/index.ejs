<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin & AI Assistant</title>
    <link rel="icon" href="./admin.png" type="image/png">
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap');
        body { font-family: 'Inter', sans-serif; }
        .chat-message { animation: fadeIn 0.3s ease-in; }
        @keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }
        .btn-hover { transition: all 0.3s ease; }
        .btn-hover:hover { transform: translateY(-2px); box-shadow: 0 10px 20px rgba(0,0,0,0.1); }
        
        .chatbot-container {
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            transform-origin: bottom right;
        }
        .chatbot-hidden {
            opacity: 0;
            transform: scale(0.95) translateY(20px);
            pointer-events: none;
        }
        .chatbot-visible {
            opacity: 1;
            transform: scale(1) translateY(0);
        }
        
        .float-btn {
            animation: float 3s ease-in-out infinite;
        }
        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
        }
    </style>
</head>
<body class="bg-gradient-to-br from-slate-50 to-slate-100 min-h-screen">
    <!-- *navbar -->
    <header class="fixed w-full bg-white shadow-sm border-b">
        <div class="max-w-7xl mx-auto px-6 py-4">
            <div class="flex items-center justify-between">
                <div class="flex items-center gap-3">
                    <div class="w-10 h-10 bg-gradient-to-br from-blue-500 to-blue-600 rounded-lg flex items-center justify-center">
                        <span class="text-white font-bold text-xl">X</span>
                    </div>
                    <div>
                        <h1 class="text-xl font-bold text-gray-800">Gudang Dashboard</h1>
                        <p class="text-xs text-gray-500">Admin & AI Assistant</p>
                    </div>
                </div>
                <div class="text-sm text-gray-500">
                    <%= new Date().toLocaleDateString('id-ID', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' }) %>
                </div>
            </div>
        </div>
    </header>

    <!-- # content -->
    <main class="max-w-7xl mx-auto px-6 pb-8 pt-24">
        
        <div class="grid grid-cols-1 gap-6">
            
            <div class="bg-white rounded-2xl shadow-lg overflow-hidden border border-gray-100">
                <div class="bg-gradient-to-r from-blue-500 to-blue-600 px-6 py-4">
                    <h2 class="text-xl font-bold text-white flex items-center gap-2">
                        <span>🛒</span> Input Pembelian
                    </h2>
                </div>
                <!-- /! form -->
                <form action="/buy" method="POST" class="p-6 space-y-4">
                    <div>
                        <label class="block text-sm font-semibold text-gray-700 mb-2">Pilih Produk</label>
                        <select name="product_id" class="w-full border-2 border-gray-200 p-3 rounded-xl focus:border-blue-500 focus:ring focus:ring-blue-200 transition" required>
                            <option value="">-- Pilih Produk --</option>
                            <% products.forEach(prod => { %>
                                <option value="<%= prod.id %>">
                                    <%= prod.name %> (Stok: <%= prod.qty %>)
                                </option>
                            <% }) %>
                        </select>
                    </div>

                    <div>
                        <label class="block text-sm font-semibold text-gray-700 mb-2">Jumlah</label>
                        <input type="number" name="qty" min="1" placeholder="Masukkan jumlah..." 
                            class="w-full border-2 border-gray-200 p-3 rounded-xl focus:border-blue-500 focus:ring focus:ring-blue-200 transition" required>
                    </div>

                    <button type="submit" class="w-full bg-gradient-to-r from-blue-500 to-blue-600 text-white font-semibold py-3 px-6 rounded-xl btn-hover shadow-lg shadow-blue-500/30">
                        <span class="flex items-center justify-center gap-2">
                            <span>✓</span> Submit Pembelian
                        </span>
                    </button>
                </form>
            </div>
            <!-- * riwayat -->
            <div class="bg-white rounded-2xl shadow-lg overflow-hidden border border-gray-100">
                <div class="bg-gradient-to-r from-slate-700 to-slate-800 px-6 py-4">
                    <h2 class="text-xl font-bold text-white flex items-center gap-2">
                        <span>📋</span> Riwayat Transaksi
                    </h2>
                </div>
                
                <div class="overflow-x-auto">
                    <table class="w-full">
                        <thead class="bg-gray-50 border-b-2 border-gray-200">
                            <tr>
                                <th class="px-6 py-4 text-left text-xs font-bold text-gray-600 uppercase tracking-wider">Produk</th>
                                <th class="px-6 py-4 text-center text-xs font-bold text-gray-600 uppercase tracking-wider">Qty</th>
                                <th class="px-6 py-4 text-center text-xs font-bold text-gray-600 uppercase tracking-wider">Status</th>
                                <th class="px-6 py-4 text-center text-xs font-bold text-gray-600 uppercase tracking-wider">Aksi</th>
                            </tr>
                        </thead>
                        <tbody class="divide-y divide-gray-100">
                            <% if(purchases.length === 0) { %>
                                <tr>
                                    <td colspan="4" class="px-6 py-12 text-center text-gray-400">
                                        <div class="flex flex-col items-center gap-2">
                                            <span class="text-4xl">📦</span>
                                            <p>Belum ada transaksi</p>
                                        </div>
                                    </td>
                                </tr>
                            <% } else { %>
                                <% purchases.forEach(p => { %>
                                    <tr class="hover:bg-gray-50 transition">
                                        <td class="px-6 py-4">
                                            <div class="font-semibold text-gray-800"><%= p.name %></div>
                                        </td>
                                        <td class="px-6 py-4 text-center">
                                            <span class="bg-blue-100 text-blue-800 px-3 py-1 rounded-full text-sm font-semibold"><%= p.qty %></span>
                                        </td>
                                        <td class="px-6 py-4 text-center">
                                            <% if(p.status === 'completed') { %>
                                                <span class="inline-flex items-center gap-1 bg-green-100 text-green-700 px-3 py-1 rounded-full text-sm font-semibold">
                                                    <span>✓</span> Completed
                                                </span>
                                            <% } else { %>
                                                <span class="inline-flex items-center gap-1 bg-red-100 text-red-700 px-3 py-1 rounded-full text-sm font-semibold">
                                                    <span>✕</span> Cancelled
                                                </span>
                                            <% } %>
                                        </td>
                                        <td class="px-6 py-4 text-center">
                                            <% if(p.status === 'completed') { %>
                                                <form action="/cancel/<%= p.id %>" method="POST" onsubmit="return confirm('Yakin ingin membatalkan transaksi ini?')">
                                                    <button type="submit" class="text-red-600 hover:text-red-700 font-semibold text-sm transition">
                                                        Cancel
                                                    </button>
                                                </form>
                                            <% } else { %>
                                                <span class="text-gray-400">-</span>
                                            <% } %>
                                        </td>
                                    </tr>
                                <% }) %>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>

        </div>
    </div>

    <!-- * btn chatbot -->
    <button onclick="toggleChatbot()" 
        id="chatbot-btn"
        class="fixed bottom-6 right-6 w-16 h-16 bg-gradient-to-r from-green-500 to-green-600 rounded-full shadow-2xl flex items-center justify-center text-white text-2xl hover:scale-110 transition-transform float-btn z-50">
        🤖
    </button>

    <div id="chatbot-container" class="chatbot-container chatbot-hidden fixed bottom-24 right-6 w-[400px] z-50">
        <div class="bg-white rounded-2xl shadow-2xl overflow-hidden border border-gray-200">
            <div class="bg-gradient-to-r from-green-500 to-green-600 px-6 py-4 flex items-center justify-between">
                <div>
                    <h2 class="text-lg font-bold text-white flex items-center gap-2">
                        <span>🤖</span> AI Assistant
                    </h2>
                    <p class="text-green-100 text-xs mt-0.5">Tanya apa saja...</p>
                </div>
                <button onclick="toggleChatbot()" class="text-white hover:bg-white/20 rounded-lg p-2 transition">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
                    </svg>
                </button>
            </div>
            
            <div id="chat-box" class="h-[450px] overflow-y-auto p-4 bg-gradient-to-b from-gray-50 to-white space-y-3">
                <div class="flex items-center justify-center h-full">
                    <div class="text-center text-gray-400">
                        <div class="text-5xl mb-3">💬</div>
                        <p class="text-sm">Mulai percakapan dengan AI</p>
                    </div>
                </div>
            </div>

            <div class="border-t bg-gray-50 p-4">
                <div class="flex gap-2">
                    <textarea 
                        id="user-input" 
                        rows="1"
                        class="flex-1 border-2 border-gray-200 p-3 rounded-xl focus:border-green-500 focus:ring focus:ring-green-200 transition resize-none overflow-hidden" 
                        placeholder="Ketik pesan..."
                        onkeypress="if(event.key==='Enter' && !event.shiftKey) { event.preventDefault(); sendMessage(); }"
                        oninput="autoResize(this)"></textarea>
                    <button onclick="sendMessage()" 
                        class="bg-gradient-to-r from-green-500 to-green-600 text-white p-2 rounded-full font-semibold btn-hover shadow-lg shadow-green-500/30 self-end">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8"/>
                        </svg>
                    </button>
                </div>
            </div>
        </div>
    </div>

    <script>
        let firstMessage = true;
        let chatbotOpen = false;
        //  toggle chatbot
        function toggleChatbot() {
            const container = document.getElementById('chatbot-container');
            const btn = document.getElementById('chatbot-btn');
            
            chatbotOpen = !chatbotOpen;
            
            if(chatbotOpen) {
                container.classList.remove('chatbot-hidden');
                container.classList.add('chatbot-visible');
                btn.style.transform = 'scale(0.9)';
                document.getElementById('user-input').focus();
            } else {
                container.classList.remove('chatbot-visible');
                container.classList.add('chatbot-hidden');
                btn.style.transform = 'scale(1)';
            }
        }

        function autoResize(textarea) {
            textarea.style.height = 'auto';
            textarea.style.height = Math.min(textarea.scrollHeight, 120) + 'px';
        }

        async function sendMessage() {
            const input = document.getElementById('user-input');
            const chatBox = document.getElementById('chat-box');
            const text = input.value.trim();
            
            if(!text) return;

            if(firstMessage) {
                chatBox.innerHTML = '';
                firstMessage = false;
            }

            chatBox.innerHTML += `
                <div class="chat-message flex justify-end mb-3">
                    <div class="bg-gradient-to-r from-blue-500 to-blue-600 text-white px-4 py-3 rounded-2xl rounded-tr-sm max-w-[85%] shadow-md">
                        <p class="text-sm whitespace-pre-wrap break-words">${escapeHtml(text)}</p>
                    </div>
                </div>
            `;
            input.value = '';
            input.style.height = 'auto';
            chatBox.scrollTop = chatBox.scrollHeight;

            const loadingId = 'loading-' + Date.now();
            chatBox.innerHTML += `
                <div id="${loadingId}" class="chat-message flex justify-start mb-3">
                    <div class="bg-gray-200 px-4 py-3 rounded-2xl rounded-tl-sm shadow-md">
                        <div class="flex gap-1">
                            <span class="w-2 h-2 bg-gray-400 rounded-full animate-bounce" style="animation-delay: 0ms"></span>
                            <span class="w-2 h-2 bg-gray-400 rounded-full animate-bounce" style="animation-delay: 150ms"></span>
                            <span class="w-2 h-2 bg-gray-400 rounded-full animate-bounce" style="animation-delay: 300ms"></span>
                        </div>
                    </div>
                </div>
            `;
            chatBox.scrollTop = chatBox.scrollHeight;

            try {
                const res = await fetch('/api/chat', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ message: text })
                });
                
                const data = await res.json();

                document.getElementById(loadingId).remove();

                chatBox.innerHTML += `
                    <div class="chat-message flex justify-start mb-3">
                        <div class="bg-gradient-to-r from-gray-100 to-gray-200 px-4 py-3 rounded-2xl rounded-tl-sm max-w-[85%] shadow-md">
                            <p class="text-sm text-gray-800 whitespace-pre-wrap break-words">${escapeHtml(data.reply)}</p>
                        </div>
                    </div>
                `;
            } catch(error) {
                document.getElementById(loadingId).remove();
                chatBox.innerHTML += `
                    <div class="chat-message flex justify-start mb-3">
                        <div class="bg-red-100 px-4 py-3 rounded-2xl rounded-tl-sm max-w-[85%] shadow-md">
                            <p class="text-sm text-red-800">❌ Terjadi kesalahan. Coba lagi.</p>
                        </div>
                    </div>
                `;
            }

            chatBox.scrollTop = chatBox.scrollHeight;
        }

        function escapeHtml(text) {
            const div = document.createElement('div');
            div.textContent = text;
            return div.innerHTML;
        }
    </script>
</body>
</html>